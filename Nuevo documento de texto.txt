iniciar un proyecyo maven: 
mvn archetype:generate -Dfilter=maven-archetype-quickstart

compilar: mvn compile 
(y se genera el target)

generar el package: mvn package

limpiar: mvn clean

hacer el tp desde la aplicacion maven.